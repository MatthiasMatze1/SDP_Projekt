name: develop_action

on:
  push:
    branches: [ "develop"]


jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --platform linux/arm/v6,linux/amd64 --tag projekt:$(date +%s)
