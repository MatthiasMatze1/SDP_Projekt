name: develop actoins 

on:
merge:
  branches: [ "develop" ]
pull_request:
   branches: [ "develop" ]


    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag projekt:test
  
